# Arquivo de exemplo de variáveis de ambiente
# Copie este arquivo para .env.local e preencha com seus valores

# === Firebase Configuration ===
# Obter em: Firebase Console > Project Settings > General > Your apps > Web app
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# === Firebase App Check (reCAPTCHA v3) ===
# Obter em: https://www.google.com/recaptcha/admin
# Tipo: reCAPTCHA v3
# IMPORTANTE: Use a SITE KEY (não a Secret Key)
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_v3_site_key_here

# === Gemini API Key (APENAS para desenvolvimento local) ===
# NUNCA use no frontend! Apenas para testes locais das Cloud Functions
# Em produção, configurar via: firebase functions:secrets:set GEMINI_API_KEY
GEMINI_API_KEY=your_gemini_api_key_here

# === Environment ===
VITE_ENV=development

# === Instruções de Setup ===
# 1. Copie este arquivo: cp .env.example .env.local
# 2. Preencha todos os valores
# 3. NUNCA commite o arquivo .env.local no Git
# 4. Para produção, use variáveis de ambiente do Firebase Hosting
